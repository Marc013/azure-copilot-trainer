source "https://rubygems.org"

# Use this Gemfile to preview the site locally with `bundle exec jekyll serve`.
# This is NOT used by GitHub Pages — GitHub Pages manages its own gem environment.

gem "github-pages", group: :jekyll_plugins
gem "just-the-docs"

group :jekyll_plugins do
  gem "jekyll-remote-theme"
  gem "jekyll-seo-tag"
end
